spring:
  application:
    name: kento_shopping
  profiles:
    active: dev
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 100ms
      connect-timeout: 100ms
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

